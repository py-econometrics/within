[workspace]
members = [
    "crates/schwarz-precond",
    "crates/within",
    "crates/within-py",
]
resolver = "2"

[workspace.dependencies]
approx-chol = { path = "../approx-chol/crates/approx-chol", features = ["serde"] }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
